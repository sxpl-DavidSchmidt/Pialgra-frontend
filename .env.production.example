SERVICE_DOMAIN=127.0.0.1
# Optional: all accepted hosts, separated by spaces (include SERVICE_DOMAIN).
# SERVICE_DOMAINS="pialgra.app www.pialgra.app 100.114.158.125"
# For IP access, choose the IP as the certificate fallback for clients without SNI.
# TLS_DEFAULT_SERVER_NAME=100.114.158.125
# CORS_ALLOWED_ORIGINS=https://pialgra.app,https://www.pialgra.app,https://100.114.158.125
ACME_EMAIL=admin@example.com
DB_PASSWORD=changeme
BACKEND_PATH=../Pialgra-backend # Repositories are expected to be siblings on the same machine
POSTGRES_VOLUME=pialgra-production-postgres
